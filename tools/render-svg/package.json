{
  "name": "@brand-icons/render-svg",
  "version": "0.0.0",
  "private": true,
  "description": "Tiny CLI that rasterizes an SVG to PNG (used by icon-fetcher / icon-builder / icon-reviewer agents for visual fidelity checks).",
  "type": "module",
  "bin": {
    "render-svg": "./render.mjs"
  },
  "scripts": {
    "typecheck": "echo 'no typecheck'",
    "test": "echo 'no test'",
    "clean": "echo 'no build artifacts'"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
