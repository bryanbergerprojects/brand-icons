# @brand-icons/build-icons

Internal CLI that consumes `icons/*/` source files and generates:

- `@brand-icons/core` SVG strings + manifest.
- React, Vue, Svelte, Web Components packages.

Implemented in sprint 3 (see `../../PLAN.md`).
