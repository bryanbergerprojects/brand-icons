{
  "name": "@brand-icons/build-icons",
  "version": "0.0.0",
  "private": true,
  "description": "Pipeline that turns icons/ source files into framework packages.",
  "type": "module",
  "bin": {
    "build-icons": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "eta": "^3.5.0",
    "svgo": "^3.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
