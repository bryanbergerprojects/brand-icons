---
"@brand-icons/core": patch
"@brand-icons/react": patch
"@brand-icons/vue": patch
"@brand-icons/svelte": patch
"@brand-icons/wc": patch
---

Correct **Google Chat** 2017 millésime: rewrite the `color.svg` and `mono.svg` from the official Wikimedia source (`Google Chat logo (2017-2020).svg`) — bubble + `@` glyph geometry now taken verbatim from the official file (rescaled to 24×24) — and fix the palette to the actual Material Design tokens (`#00897B` Teal 600, `#F5F5F5` Grey 100) instead of the previously sampled approximations.
