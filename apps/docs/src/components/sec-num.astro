---
type Props = {
  kicker: string;
  label: string;
  num?: string;
  accent?: boolean;
  labelClass?: string;
};

const { kicker, label, num, accent = false, labelClass = 'text-h2' } = Astro.props;
---

<div class="mb-12">
  <div class="mb-3.5 flex items-baseline gap-3.5 font-mono text-mono font-bold uppercase tracking-mono text-ink-soft">
    {num ? <span class:list={['font-mono font-bold', accent ? 'text-accent' : 'text-ink']}>{num}</span> : null}
    <span>{kicker}</span>
  </div>
  <div class:list={['max-w-[1100px] font-extrabold tracking-display-sm text-ink', labelClass]}>
    {label}
  </div>
</div>
